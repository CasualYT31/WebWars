/**
 * @file map.mjs
 * Defines the class that represents a single map made of tiles, with units on them, etc.
 */

/**
 * A single map of tiles, with faction information, units, a weather state, and much more.
 */
export default class Map {}
