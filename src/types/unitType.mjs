/**
 * @file unitType.mjs
 * Defines the properties required for Unit types.
 */

import ObjectType from "#src/types/objectType.mjs";

/**
 * A unit is a single entity on a map that is usually owned by a faction.
 * @interface
 */
export default class UnitType extends ObjectType {}
