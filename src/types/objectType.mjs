/**
 * @file objectType.mjs
 * Defines the base class for all the different sorts of object type that the game engine supports.
 */

/**
 * Everything on a map, including countries, commanding officers, etc. is an object, and objects are classified into
 * types.
 * This base class defines everything that is common between all object types.
 */
export default class ObjectType {}
