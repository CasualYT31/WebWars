/**
 * @file playableMaps.mjs
 * Contains the model responsible for managing WebWars maps during gameplay.
 */

import Model from "#src/mvc/model.mjs";

/**
 * Stores map objects as they are read by the MapManager model and allows clients to play games on them.
 */
export default class PlayableMaps extends Model {}
